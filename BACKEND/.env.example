# Create a .env file in the BACKEND folder.
# Copy this file and replace the placeholder with your own Django secret key.

SECRET_KEY='Your Secret Key here'
ALLOWED_HOSTS=localhost,127.0.0.1,testserver